---
interface Props { label: string; value: string; source?: string | undefined; }
const { label, value, source } = Astro.props;
---

<span class="metric-badge" title={source}><span>{label}</span> {value}</span>
